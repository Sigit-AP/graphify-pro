# Loop Budget — graphify 50x project

Per-iteration caps (self-throttle at 80%, hard-exit at 100%):

- max sub-agent spawns / iteration: 2
- max fix attempts / target: 3 (then escalate to human)
- report-only mode: when nothing actionable remains in a phase

## Run Log
(appended one JSON line per iteration)
